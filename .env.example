# Server
BIND=0.0.0.0:8080
RUST_LOG=info

# Paths (opsional, default: storage & hls_tmp)
STORAGE_DIR=/data
HLS_ROOT=/tmp/hls

# Database (runtime: service "db" di docker compose)
DATABASE_URL=postgres://ppv:secret@db:5432/ppv_stream

# HLS / security
HMAC_SECRET=change-this-secret-key
# NAMA VARIABEL HARUS SAMA DGN config.rs -> session_token_ttl (detik)
SESSION_TOKEN_TTL=600
HLS_SEGMENT_SECONDS=6
WATERMARK_FONT=/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf

# Bootstrap admin (opsional)
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=ChangeMe123!
